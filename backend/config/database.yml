default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
  username: postgres

development:
  <<: *default
  database: stride_development

test:
  <<: *default
  database: stride_test

production:
  <<: *default
  url: <%= ENV["DATABASE_URL"] %>
